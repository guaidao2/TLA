# 奇点-薛定谔世界模型（Singularity-Superposition World Model, SSW）

独立子项目（2026-08-11 首轮）：把**奇点神经元**放进**薛定谔叠加态世界模型**
（FakeAGI-LLM 的叠加态世界模型概念，本地重实现）——隐藏规则 + 事件计时的场景。
TLA 是否引用另行决定（当前未接线）。

## 组合设计（判据先于实现）

| 零件 | 角色 |
|---|---|
| 薛定谔叠加 | K 个分支（"可能世界"），amplitude=softmax(−预测误差) 坍缩，熵高/最优分支持续失败时分裂 |
| 奇点时钟银行 | 分支状态 = N 个独立奇点细胞（无循环），事件 → 分级暴胀，安静 → 指数衰减（λ=0.08, τ≈12.5）；特征 = −ln(h+ε)/4.6（对数线性化：衰减对时间线性） |
| 日程专家 | 分支 i 读出头先验 = 规则 i 的事件间隔（监督校准 4 epoch——实测 2 欠训、8+ 过拟合） |
| 对照 | LTC 泄漏积分基板 / 无状态基板（同一读出头，只换状态机制） |

环境：隐藏规则事件世界——3 条规则（事件间隔 5±1 / 10±2 / 20±5 tick），观测
=[event_flag, 弱规则信号]，目标=[下一事件, 归一化距下次事件]。规则隐藏 → 单值模型
学不会，叠加模型靠事件时刻坍缩。

## 判据与实测（标准量 n_ep=20，判据锁死）

| 判据 | 标准 | 实测 | 裁决 |
|---|---|---|---|
| SW-1 时间戳解码 | sing < ltc < ff | 0.0762 / 0.0274 / 0.0297 | **FAIL（锁死）** |
| SW-2 叠加时间预测 | sing < ltc < ff | 0.0129 / 0.0166 / 0.0123 | **FAIL（锁死，边缘）** |
| SW-3a 坍缩正确性 | sing ≥ ff + 0.05 | 0.86 vs 0.71 | **PASS** |
| SW-3b 分裂生长 | 分裂 + n=3 + 新分支>0.3 | t≈9 / n=3 / 0.72 | **PASS** |
| SW-4 单值对照 | k1 > 1.2× k3 | 0.0380 vs 0.0129 = 2.95× | **PASS** |

## 净结论（首轮，2026-08-11）

1. **叠加机制全验证**：坍缩（SW-3a）、分裂生长（SW-3b）、单值失败（SW-4 2.95×）——
   隐藏规则 + 事件计时场景里叠加世界模型成立。
2. **奇点时间状态有真实增益**：SW-3a sing 0.86 vs ff 0.71（时间状态在日程常数之上
   让坍缩更准）。
3. **时间解码未超日程常数（诚实负结果）**：SW-1/2 的 learned MLP 解码器败给 ff 的
   常数预测——slow 规则（20±5 tick）宽域解码是瓶颈（基板解码能力已由奇点论文
   SN-3 用解析反演单独验证 2.59%，瓶颈在 learned 解码器不在基板）。
4. **适应-vs-分裂权衡（发现）**：在线训练下专家直接漂移适应新规则（取代分裂）；
   分裂是"提交型假设"（头冻结）才出现的机制——容量复用优先于生长。

## 首轮修正记录（判据未改，实现/设置修正）

- 事件 flag 噪声 5% → 0：翻转噪声重置所有基板时钟，产生矛盾训练对（噪声鲁棒性另列）；
- 时钟银行替代含 W_h 的 substrate：复现级联恒热（热细胞持续驱动邻细胞）；
- 坍缩温度 1.0 → 0.02：归一化时间误差量级小，原温度下 softmax 平坦（坍缩不集中）；
- 分裂触发：熵信号 → 最优分支持久失败率（尖锐温度杀死熵信号；均值对偶发匹配敏感）；
- SW-3b 公平设定：专家头冻结（否则适应取代分裂）；
- SW-3a 判据前提修正：原"ff < 0.5"错误（ff 靠日程常数也能坍缩 0.71）→ 改时间状态增益；
- split() 硬编码 head_hidden=32 与模型 64 不匹配的 bug 修复。

## 运行

```bash
PYTHONIOENCODING=utf-8 python -m ssw.criteria.run_ssw   # 标准量（~60s）
python -m pytest tests/test_ssw.py                       # 锁死测试（轻量 ~30s）
```
